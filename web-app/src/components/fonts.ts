import { Inter, Atkinson_Hyperlegible } from "next/font/google";

export const inter = Inter({ subsets: ["latin"] });

export const mainFont = Atkinson_Hyperlegible({
  subsets: ["latin"],
  weight: ["400", "700"],
});
